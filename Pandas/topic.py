import pandas as pd
# how big is the data what are the names of the columns what are the data types 
#shape and columns 
data = {    'Name': ['Alice', 'Bob', 'Charlie'],
            'Age': [25, 30, 35],
            'City': ['New York', 'Los Angeles', 'Chicago']
        }
df = pd.DataFrame(data)
print(df)
print(f'DataFrame shape: {df.shape}')  # Print the shape of the DataFrame
print(f'Columns: {df.columns}')  # Print the column names
# 1 select specific columns square brackets  2 filter rows 3 combine multiple conditions 4 sort data 5 group by 6 pivot table 7 merge join concat 8 handle missing values
# 9 apply functions 10 time series data 11 read write files 12 visualize data
# 13 handle categorical data 14 handle text data 15 handle dates and times 16 handle large datasets
# 17 handle hierarchical data 18 handle multi-index data 19 handle time zones 20 handle JSON data
# 21 handle XML data 22 handle SQL databases 23 handle web scraping 24 handle APIs 25 handle big data
# 26 handle machine learning data 27 handle deep learning data 28 handle natural language processing data
# 29 handle image data 30 handle audio data 31 handle video data 32 handle geospatial data
# 33 handle network data 34 handle graph data 35 handle time series forecasting data 36 handle anomaly detection data
# 37 handle recommendation system data 38 handle sentiment analysis data 39 handle topic modeling data